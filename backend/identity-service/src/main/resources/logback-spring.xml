<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <springProperty name="appName" source="spring.application.name" defaultValue="identity-service"/>

    <!-- Plain pattern for local dev. Prod profile uses JSON via logstash-logback-encoder
         (add the dep when you wire your log aggregator). -->
    <property name="LOGPAT" value="%d{ISO8601} %-5level %logger{30} ${appName} ${CORRELATION:-[]} - %msg%n"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder><pattern>%d{ISO8601} %-5level [%X{requestId:-} t=%X{tenantId:-} u=%X{userId:-}] %logger{30} - %msg%n</pattern></encoder>
    </appender>

    <root level="INFO"><appender-ref ref="CONSOLE"/></root>
    <logger name="com.handa.tms" level="DEBUG"/>
</configuration>
