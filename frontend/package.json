{
  "name": "@tms/frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend monorepo. Each subdirectory is an independently deployable Module Federation host or remote.",
  "scripts": {
    "dev": "pnpm -r --parallel --filter './shell' --filter './mfe-*' run dev",
    "build": "pnpm -r --filter './shared' --filter './design-system' run build && pnpm -r --filter './shell' --filter './mfe-*' run build",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r exec rm -rf dist node_modules/.vite"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "prettier": "^3.3.3",
    "eslint": "^9.13.0"
  },
  "packageManager": "pnpm@9.12.2"
}
