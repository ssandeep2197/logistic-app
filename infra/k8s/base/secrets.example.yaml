# TEMPLATE — never commit real secret values. Apply this on the cluster:
#   kubectl create secret generic tms-jwt --from-literal=signing-key="$(openssl rand -hex 64)" -n tms
#   kubectl create secret generic tms-postgres \
#     --from-literal=identity-password=...     \
#     --from-literal=operations-password=...   \
#     ... -n tms
apiVersion: v1
kind: Secret
metadata:
  name: tms-jwt
  namespace: tms
type: Opaque
stringData:
  signing-key: "REPLACE_ME_WITH_64_BYTES_OF_HEX"
---
apiVersion: v1
kind: Secret
metadata:
  name: tms-postgres
  namespace: tms
type: Opaque
stringData:
  identity-password: "REPLACE_ME"
  operations-password: "REPLACE_ME"
  dispatch-password: "REPLACE_ME"
  tracking-password: "REPLACE_ME"
  finance-password: "REPLACE_ME"
  payroll-password: "REPLACE_ME"
  compliance-password: "REPLACE_ME"
  documents-password: "REPLACE_ME"
  reporting-password: "REPLACE_ME"
  notification-password: "REPLACE_ME"
---
apiVersion: v1
kind: Secret
metadata:
  name: tms-anthropic
  namespace: tms
type: Opaque
stringData:
  api-key: "REPLACE_ME"
---
apiVersion: v1
kind: Secret
metadata:
  name: tms-s3
  namespace: tms
type: Opaque
stringData:
  access-key: "REPLACE_ME"
  secret-key: "REPLACE_ME"
